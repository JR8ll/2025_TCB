#include <iostream>

#include "Functions.h"

LogPriority Logger::verbosity = Info;
const char* Logger::filepath = "log.txt";
Logger TCB::logger;

int main(int argc, char* argv[]) {
	return EXIT_SUCCESS;
}